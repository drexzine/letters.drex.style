---
# =============================================================================
# GUEST WRITER TEMPLATE — copy this file to add a guest voice to Circle Letters.
#
# Steps:
#   1. Copy this file to _authors/<slug>.md  (e.g. _authors/ada.md). Pick a short,
#      lowercase, hyphen-free `slug` — it becomes the page URL: /authors/<slug>/.
#   2. Add a matching entry to _data/authors.yml under the SAME key as `slug`
#      (copy the documented `guest:` example there). That entry holds the photo,
#      role, accent, and bio used on the byline chip + this page header.
#   3. In the guest's post front matter, set `author: <slug>` so their letters
#      link here and show up in the "Letters from …" list below.
#   4. Drop a square headshot at the `photo:` path you set in authors.yml.
#
# The `_` prefix on this filename keeps Jekyll from publishing the template itself.
# =============================================================================
layout: author
slug: guest          # MUST match the key you add in _data/authors.yml
title: "Guest Writer"
name: "Guest Writer"
---

A sentence or two in the guest's own voice — who they are, what they care about,
and why they're writing for Circle Letters. Keep it warm and plain. Everything
else (photo, role, accent, short bio) comes from their entry in _data/authors.yml.
