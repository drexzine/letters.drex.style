# ===================================================================
# Drex blog — letters.drex.style ("Circle Letters")
# Chielo's founder/CEO essays + guest writers, in the Drex zine aesthetic.
# GitHub-Pages-native Jekyll build. Only Pages-whitelisted plugins.
# ===================================================================

title: "drex."
tagline: "Circle Letters, made public."
description: >-
  Circle Letters — founder essays, canon, and lore.
  Learn your craft, among others. A place for communion.
author: "Chielo Mbaezue"

url: "https://letters.drex.style"
baseurl: ""

lang: en
timezone: America/Los_Angeles

# Pretty permalinks: /2026/05/the-correspondence/
permalink: /:year/:month/:slug/

# --- markdown / build ---
markdown: kramdown
highlighter: rouge
kramdown:
  input: GFM
  hard_wrap: false
  smart_quotes: ["lsquo", "rsquo", "ldquo", "rdquo"]

# --- collections ---
collections:
  authors:
    output: true
    permalink: /authors/:path/

# --- collections / defaults ---
defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
  - scope:
      path: ""
      type: authors
    values:
      layout: author
  - scope:
      path: ""
    values:
      layout: default
      # Default social-share preview image. jekyll-seo-tag (2.8.0) reads
      # `page.image` only, so we set it as a front-matter default → every
      # page emits og:image + twitter:image. Chielo's founder portrait;
      # `url` above makes it an absolute URL. A post may override `image`.
      image: /assets/img/team/chielo.jpg

# --- pagination-free home; posts sorted newest-first by Jekyll ---
show_excerpts: false

# --- plugins (all GitHub-Pages-whitelisted) ---
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

feed:
  path: /feed.xml

# --- Email newsletter feed (Kit RSS-to-email) ---
# ALL posts are newsletters. A letter appears in /email-feed.xml if its date is on/after
# `start_date` (the launch line — this keeps the pre-launch archive out of the feed).
# Opt a single post OUT with `newsletter: false` in its front matter.
# Kit is draft-only (auto-send OFF), so a human still reviews + Sends every draft.
email:
  start_date: 2026-06-23

# --- social / SEO ---
social:
  name: "Drex"
  links:
    - https://drex.style
    - https://instagram.com/drexcircles

# --- build hygiene ---
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
  - README.md
  - PLAN.md
  - FOR-CHIELO.md
  - CLAUDE.md
  - scripts
  - .githooks
  - .jekyll-cache
  - node_modules

sass:
  style: compressed
